# Taste
- Wants deep, systematic bug finding and full review of the entire codebase (source, tests, config) before work is considered done — not just surface-level checks. Explicitly demands "zero bugs" across every command/path, so coverage must be exhaustive (all safe commands, argument-validation paths, mutating commands, and regression suites), not just the happy path. Repeatedly extends this deep QA to named additional surfaces: database schemas (no schema bugs, no unused tables, migrations valid) and all official Konoha skills. Confidence: 0.9
- Expects all documentation, rules, diagrams, compliance reports, and skill definitions to match the codebase 100% — treats generated instruction blocks, docs, maintenance-skill knowledge, and versioned compliance references as audit targets that must stay in sync with actual code, tool names, exports, and release versions. Compliance links should resolve to the actual report artifact for the stated release, using the exact `docs/SecurityCompliance/security_compliance_report_google_policy_<version>_<YYYY-MM-DD>.md` filename convention; here `<version>` means the audited project/release version (for example, Konoha `2.0.0`), not the Google Policy revision. Stale or mismatched references must be corrected, and the naming rule should be propagated to source and deployed maintenance-skill copies. Explicitly asks for knowledge/rules to be updated into maintenance skills whenever behavior changes, and for diagrams to be kept current with the runtime (verified node definitions, client surfaces, persistence scope, tool counts). Confidence: 0.96
- Wants fresh-install setup to fully configure every supported client (Antigravity CLI/IDE, Claude Code, Cursor, OpenCode, Command Code) with the complete stack — Konoha MCP, Semble MCP, and RTK rules — consistently, so no detected client is left partially or inconsistently integrated. Repeats this expectation whenever listing the clients that must work properly. Confidence: 0.85
- Wants final QA reports to be honest and evidence-based rather than sweeping claims: does not want "zero bugs" asserted without proof, expects remaining limitations to be reported explicitly, and accepts only verified fixes (discarding review outputs that failed or were unreliable). Also reports leftover work (e.g., source/deployed skill-tree drift) as a separate follow-up rather than silently expanding scope or deleting deployed-only content. Confidence: 0.75
- When fixing content that exists in multiple places (e.g., official skill templates), expects the fix applied consistently to the source template and every deployed copy (`.agents/`, `.cursor/`) with location-correct relative paths, then verified with parity/link checks — not just patched in one location. Confidence: 0.75
- Wants the system resilient to partial environments across the whole CLI surface, not just fresh install: each client is detected independently, missing clients are simply skipped (never requiring all supported clients to be present), and a missing optional component like RTK produces a warning rather than failing installation. Diagnostic commands must follow the same rule — `konoha status` reports absent optional clients as SKIPPED (not errors) and `konoha doctor` only checks/repairs installed clients so missing optional clients never affect its health verdict. Explicitly asks to verify that status/doctor behave correctly when clients are skipped, and expects the change to be validated by running the commands and the regression suite. Confidence: 0.9
- Prefers preserving existing features and public tool names for backward compatibility; when a legacy capability such as `build_with_image_design` is still wanted, retain it as a supported alias or compatibility path rather than removing it in favor of a newer API. Confidence: 0.95
- Wants integrations to be verified as actually invoked and their outputs explained, especially when a third-party tool overlaps with native Konoha capabilities; distinguish real integration failures/zero results from parser or invocation problems and identify equivalent tools clearly. Confidence: 0.82
- Prefers removing redundant wrapper capabilities when an external integration already provides the canonical feature: semantic/code search should use direct Semble tools rather than a duplicated Konoha `search_file` wrapper. Such removals must be end-to-end across source, schemas, policies, docs, tests, deployment artifacts, and live tool lists, with direct Semble usage verified. Confidence: 0.92
- For duplicate CLI/UI output, prefers the smallest targeted fix: remove only the redundant top banner from the affected command while preserving the banner for other invocation modes and avoiding unrelated behavior changes. Expects the visible output and diff to be checked afterward. Confidence: 0.9
- Wants project-maintenance/development guidance kept developer-only and not exposed as end-user/client skills (including Command Code, Cursor, Claude, OpenCode, or Antigravity); clients should receive only operational MCP tools and official user-facing agent skills. Confidence: 0.95
- Prefers consolidating overlapping skills into one canonical skill, with routing, client loading, fresh-install auto-deployment, and source/deployed parity verified before removing the obsolete skill directory. Confidence: 0.94
- Expects every architecture/system diagram to be professionally structured, clear, and maintained in editable Draw.io format, with all existing diagrams represented and checked against the implemented system. Confidence: 0.95
- Wants Mermaid diagrams retained alongside the canonical Draw.io diagrams in README and documentation Markdown files, with polished, readable styling rather than replacing Mermaid with links only. Mermaid nodes should have enough width/spacing for labels to avoid clipping, using deliberate line breaks where needed; parser-sensitive syntax (such as literal pipe characters inside node labels) must be avoided, with all Mermaid blocks render-validated and protected by regression checks. Expects Draw.io arrow routing to be inspected and corrected for overlaps/crossings. Confidence: 0.96
- Prefers aggressive repository hygiene after changes: identify and remove genuinely unused patch, temporary, and noisy artifacts, clean leftover trash after fixes, and encode the cleanup workflow in the developer-only maintenance skill. Deletion must be dependency-checked so referenced or production files are not removed. Confidence: 0.93
- Prefers work to resume from the existing task checklist and continue unfinished items without restarting or repeating completed work. Confidence: 0.85
